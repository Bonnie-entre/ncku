#include <stdio.h>

int n, c;

int code(int x){
	if (x==1) { return 1; }
	if (x%2==0) { return code(x/2); }
    if (x%2==1) { return code(x+1)+code(x-1); }
    return 0;
}

int main () {
    
    scanf("%d", &n);
    printf("%d", code(n));
    
	return 0;
}
