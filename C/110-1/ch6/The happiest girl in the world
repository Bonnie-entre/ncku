#include<stdio.h>

int main () {
    float h, d,x;
    int t=1;
    scanf("%f", &h);
    printf("%.1f\n", h);
    d = 0.5*9.8*t*t;

    while(h-d>0)
    {
      x=h-d;
      if((int)(x*100)%10>5) { printf("%.1f\n", h-d+0.05); }
      else { printf("%.1f\n", h-d); }
      //printf("%f\n\n", h-d);
      t+=1;
      d = 0.5*9.8*t*t;
    }
        
    return 0;
}

