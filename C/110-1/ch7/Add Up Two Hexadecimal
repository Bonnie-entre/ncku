#include<stdio.h>


int main () {
    unsigned a, b, h;

    //進位-輾轉相除法
    scanf("%x %x", &a, &b);
    if (a+b>0xFFFF) {printf("OVERFLOW!");}  //a+b相加可直接計算出數字，不受16進位輸入影響
    else {
      printf("%d", a+b);  //用%d or %u都可以
    }
    
    return 0;
}


