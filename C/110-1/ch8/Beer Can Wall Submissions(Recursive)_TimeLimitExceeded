#include <stdio.h>

int f(int x){
	if (x==1) { return 1;}
    if (x==2) { return 2;}
    return f(x-2)+f(x-1);
}

int main () {
    
    int w;
    scanf("%d", &w);
    printf("%d", f(w/10));
    
    return 0;
}
