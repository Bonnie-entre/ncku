#include <stdio.h>

int main(void){
    double x,a[31], sum=1;
    int d;
    
    scanf("%lf %d", &x, &d);
    a[0]=1;
    for(int i=1; i<d+1; i++)
    {
        a[i]=x*a[i-1];
        sum += a[i];
    }
    
    printf("%d", (int)a[d]);
    
    return 0;
}
